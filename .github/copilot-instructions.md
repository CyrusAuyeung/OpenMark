# Copilot Instructions

- Treat OpenMark as a local-first Markdown editor, not a generic landing page.
- Keep the first screen focused on the usable editor experience.
- Prefer small, reliable editor improvements over large feature bundles.
- Keep Markdown parsing, export, storage, and UI concerns separate as the project grows.
- Run `npm run lint` and `npm run build` after code changes.